# Tech Phase — 現状と次に進めること

更新: 2026-09-25。作業再開時はこの台帳とリモートの最新変更を確認する。
ユーザーへの報告は「今回完了／まだ残ること／次の作業／ユーザーの操作」の順に簡潔に行う。

## 2026-09-25 03:45 UTC 最新状態

- AMZN・ANET・ARM・AVGO・BE・COHR・CRDO・CRM・CRWV・GOOGLの公式IRを再確認。
  将来の確定決算日は裏づけられず、空欄・過去項目のみ・取得不能の経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- X recent-searchの3発信元は目標株価投稿だけに限定。2分間隔を終日継続した場合の
  設定上最大は2,160リクエスト/24時間だが、ローカル上限100回/24時間が先に停止する。
  運営画面へ両方を分離表示し、金額見積もりや配信遅延保証ではない旨を明記した。
- X APIは既定OFF・読取専用・非公開・自動公開なし。検索文・URL・トークンを集計へ出さない。
- Python 233件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビューブランチの`debcb0d`でVercel Previewと隔離Railwayのチェックが成功。
- 本文遮断先の次回単一復旧プローブは2026年9月25日15:27 JST以降。今回は早期アクセスを行っていない。

## 2026-09-25 02:43 UTC 最新状態

- NBIS・NOW・NVDA・ORCL・PANW・PLTR・SKHY・SNOW・VRT・AAPLの公式IRを再確認。
  将来の確定決算日は裏づけられず、空欄・過去項目のみ・取得不能の経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- X recent-searchは既定OFF・読取専用・非公開・自動公開なしを維持し、通信前に永続的な
  24時間リクエスト上限を予約する安全弁を追加。既定100回、設定範囲1-10,000回で、
  トークン・検索文・URLを保存しない。プロバイダー側の課金上限は別途必要。
- 先行追加されたX投稿到着時間の非公開比較を保持。初回履歴を速報遅延から除き、
  検索結果だけでは取りこぼしゼロを証明できない注意書きも維持した。
- Python 232件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビューブランチの`df9605c`でVercel Previewと隔離Railwayのチェックが成功。
- 本文遮断先の次回単一復旧プローブは2026年9月25日15:27 JST以降。今回は早期アクセスを行っていない。

## 2026-09-25 01:59 UTC 最新状態

- AVGO・BE・COHR・CRDO・CRM・CRWV・GOOGL・KLAC・LITE・MRVLの公式IRを再確認。
  将来の確定決算日は裏づけられず、空欄・過去項目のみ・取得不能の経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- 運営プレビューへAI下書き生成のON/OFF、非公開・人間承認必須、待機件数、24時間の
  試行・トークン上限、最短再試行時刻を追加。URL・応答本文・ヘッダー値は表示しない。
- Python 229件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビューブランチの`aea99e9`と`05a25bd`でVercel Previewと隔離Railwayのチェックが成功。
  先行していたX API対応3コミットを保持してfast-forwardした。
- 本文遮断先の次回単一復旧プローブは2026年9月25日15:27 JST以降。今回は早期アクセスを行っていない。

## 2026-09-25 00:56 UTC 最新状態

- ORCL・PANW・PLTR・SKHY・SNOW・VRT・AAPL・AMZN・ANET・ARMの公式IRを再確認。
  将来の確定決算日は確認できず、取得不能・空欄・過去項目のみの経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- AI生成APIが429または503等と有効な`Retry-After`を返した場合、同じ要求をその場で
  繰り返さず、通常バックオフより長い指定待機を永続ジョブへ引き継ぐ。秒数・HTTP日付を
  解釈し、過大値は7日で上限化する。応答本文やヘッダー値は保存しない。
- Python 229件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビューブランチの`e0df3fb`と`b92b69b`でVercel Previewと隔離Railwayのチェックが成功。
  保護付き画面本文とRailwayの公開ヘルスは接続権限・到達制限により直接再確認できていない。
- 本文遮断先の次回単一復旧プローブは既存バックオフ期限の2026年9月25日15:27 JST以降。
  今回は早期アクセスを行わず、次回に回復・再遮断と待機列の変化を確認する。

## 2026-09-24 23:49 UTC 最新状態

- CRDO・CRM・CRWV・GOOGL・KLAC・LITE・MRVL・NBIS・NOW・NVDAの公式IRを再確認。
  将来の確定決算日は確認できず、取得不能・空欄・過去項目のみの経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- 短縮された原文から作成したAI下書きを手動CLIでも正規に確認できるよう、
  `review-brief`へ`--full-source-verification`を追加した。通常のAI照合フラグとは別に監査関数へ渡し、
  フラグが欠ける承認は従来どおり拒否する。CLI経路の回帰テストと運用説明も追加した。
- Python 226件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビューブランチの`367652f`と`d0216bb`でVercel Previewと隔離Railwayのチェックが成功。
  隔離環境はready、優先5銘柄5/5正常、起動後の優先経路完了一巡8.284秒。これは内部巡回の
  実測値で、会員配信遅延の保証ではない。
- 本文923件はホスト遮断729件、アクセス制限待ち1件、定期再確認193件。取得可能0件で、
  期限到来済み回路・予約済みプローブとも0件。次の単一復旧プローブは既存バックオフ期限の
  2026年9月25日15:27 JST以降で、早期アクセスは行わない。

## 2026-09-24 22:44 UTC 最新状態

- SKHY・SNOW・VRT・AAPL・AMZN・ANET・ARM・AVGO・BE・COHRの公式IRを再確認。
  将来の確定決算日は確認できず、取得不能・空欄・過去項目のみの経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- 入力上限で原文を短縮したAI下書きは、通常のAI照合に加えて、AIへ送信されなかった範囲を
  含む原文全体と公式リンクの人間確認を承認条件に追加した。2つの確認は監査履歴へ別々に保存し、
  短縮原文の確認記録が欠ける承認済みデータは公開スナップショットから除外する。
- Python 225件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビューブランチの`fb25e67`と`8480e86`でVercel Previewと隔離Railwayのチェックが成功。
  隔離環境はready、優先5銘柄5/5正常、起動後の優先経路完了一巡8.366秒。これは内部巡回の
  実測値で、会員配信遅延の保証ではない。
- 本文923件は取得候補1件、ホスト遮断729件、アクセス制限待ち1件、定期再確認192件。
  直近バッチは1件成功・エラー0・HTTP 304再利用1件で、期限到来済み回路と予約済みプローブは0件。
- 次の実環境プローブは既存バックオフ期限の2026年9月25日15:27 JST以降。早期アクセスを
  行わず、回復・再遮断と本文待機列の変化を確認する。

## 2026-09-24 21:47 UTC 最新状態

- GOOGL・KLAC・LITE・MRVL・NBIS・NOW・NVDA・ORCL・PANW・PLTRの公式IRを再確認。
  将来の確定決算日は確認できず、取得不能・空欄・過去項目のみの経路は保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- 本文待機列は、期限到来済み回路のうち取得候補がある経路数と、今回のバッチへ実際に
  予約した単一プローブ数を匿名集計する。ホスト名・URL・HTTPコードは公開しない。
- Python 224件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
  プレビューブランチの`420ea03`と`f666a45`でVercel Previewと隔離Railwayのチェックが成功した。
- Vercel接続は対象team scopeの権限不足で保護付き画面本文を直接確認できない。GitHubの
  デプロイチェックは成功しており、権限を迂回する外部確認は行わない。
- 次の実環境プローブは既存バックオフ期限の2026年9月25日15:27 JST以降。早期アクセスを
  行わず、今回追加した集計で期限到来とバッチ予約を区別して確認する。

## 2026-09-24 20:47 UTC 最新状態

- AAPL・AMZN・ANET・ARM・AVGO・BE・COHR・CRDO・CRM・CRWVの公式IRを再確認。
  将来の確定決算日は確認できず、ANETは取得不可、ARMはHTTP 403のため保留を維持した。
  推定日と`lastCheckedOn`は追加していない。
- BLSの2026年10月2日雇用統計、10月14日CPI、10月15日PPI（各08:30 ET）と、
  FOMCの10月27-28日・12月8-9日を再確認。登録済み日程に変更なし。
- 非公開速報レビューで、公式発表日（date-onlyのまま）、監視発見時刻、本文確認時刻を分離表示。
  発見・本文確認はJSTへ変換し、AI下書きの保存時刻も生成記録へ追加した。発見時刻や確認時刻を
  公式発表時刻または配信遅延として扱わない。
- Python 224件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
  プレビューブランチの`f04c1cc`と`ce55b8e`でVercel Previewと隔離Railwayのチェックが成功した。
- 隔離環境はready、優先5銘柄5/5正常、初回完了一巡8.353秒。本文923件はホスト遮断729件、
  アクセス制限待ち1件、定期再確認193件で、取得可能0件。次の単一復旧プローブは
  2026年9月25日15:27 JST以降で、早期アクセスは行わない。

## 2026-09-24 19:57 UTC 最新状態

- TSMC公式Financial Calendarで2026年10月15日14:00 Asia/Taipeiの3Q26決算説明会を確認し、
  説明会開始のみを追加。決算資料の公開時刻は推測していない。
- MU・NFLX・SNDK・TSLAも公式予定を再確認。MSFT・QCOMは今回の表示が判定不能だったため、
  試行日のみ更新し、既存の確認日を新しい確認済み日として扱わない。
- 本文取得は通常の最優先候補を先頭に保ちつつ、バッチに余裕があれば期限到来済みホストの
  単一復旧プローブ用に枠を確保する。継続的な新着で復旧確認が埋もれることを防ぐ。
- Python 224件、画面・データ94件、lint、TypeScript、本番ビルド35ページが成功。
- プレビュー機能コミット`992b4d9`のVercel Previewと隔離Railwayチェックは両方success。
  再起動後の実環境はready、優先5銘柄5/5正常、起動後の初回完了一巡8.353秒。
  これは内部巡回の実測値で、会員配信遅延の保証ではない。
- 本文923件はホスト遮断729件、アクセス制限待ち1件、定期再確認193件。今回の再起動後は
  本文取得可能0件で、遮断先への早期アクセスや新規本文エラーは発生していない。
- 次の実環境プローブは既存バックオフ期限の2026年9月25日15:27 JST以降。アクセス制限を
  迂回した早期確認は行わない。

## 2026-09-24 18:58 UTC 最新状態

- `codex/research-preview` の機能変更コミットは `c0bab2b` まで反映済み。Vercel Previewと
  隔離Railway `research-monitor-staging` のGitHubチェックは両方success。
- 2026-09-25 03:27:28 JSTの単一復旧プローブは再度アクセス制限となり、同一ホストの
  残り729件へ追加アクセスせず再遮断。次回確認は2026-09-25 15:27:28 JST。
- 再遮断後の次巡回でも対象ホストへ通信せず、プローブ件数を増やさない回帰テストを追加。
  アクセス制限の迂回や手動の早期プローブは行わない。
- カレンダーはADBE・AMAT・AMD・ASML・CRWD・DELL・GEV・INTC・LRCX・METAを公式ページで
  再確認。既存の確定日を維持し、AMATの明記された予測日は採用しなかった。
- 通常のGit pushは実行環境にGitHubユーザー名・資格情報がなく失敗したため、認可済みGitHub
  接続で同じツリーをプレビューブランチへfast-forwardした。ローカル追跡refは更新できず、
  作業ツリーは正常だがローカルの同内容コミット列をaheadとして表示する。
- Vercelの接続は対象team scopeの権限不足でデプロイ本文を取得できない。GitHubのVercel
  statusはsuccessだが、保護付きプレビュー本文の外部確認は未完了。

## 2026-09-24 09:25 UTC 最新状態

- `codex/research-preview` の最新コミット `0e2c82c`。GitHub上のVercel Previewと隔離Railway
  `research-monitor-staging` のCIチェックは両方success。
- stagingの直近ヘルス確認ではready、優先5/5正常。DellとBroadcomは公式IR RSSの
  `ok / primary+supplemental` を確認。Sandiskは公式IR一覧が8秒でtimeoutし、SEC 8-Kへ
  fallback。遅い応答を成功扱いせず、8秒上限を維持。
- 本文キュー918件は保留・再試行・定期再確認を含む総数で、新着数ではない。直近バッチは
  0件エラーで処理中。次回は開いているホスト回路の再試行予定後に1回だけ状態確認する。
- TSMC/MRVLの403、Oracle/ArmのRSS応答不正などは未解決。公式代替元の応答が検証できるまで
  新URLを推測で追加せず、アクセス制限も迂回しない。
- このチャットからstagingの`/health`へ直接再接続を試したが、実行環境のネットワーク承認が
  成立せず取得できなかった。ここに記した稼働値は直近の成功確認時点の値。
- 継続対象は引き続き既存22銘柄。`main`、会員本番、課金、有料AI、外部通知は変更しない。

## 2026-09-24 11:02 UTC 目標株価変更ニュースの調査

- 現行の企業公式・業界記事監視には、アナリストの格付け／目標株価変更を直接取得する
  専用経路がない。今回のTipRanks X投稿は手掛かりであり、一次ソース照合済みとはしない。
- FMPがTipRanks連携のAnalyst Ratings Search APIを公開。個別アナリスト、証券会社、評価変更、
  目標価格、出典記事リンクを含む時系列データで、アクティビティフィード用途が明記されている。
- FMPの個人向け価格表示だけでは利用不可。顧客向け表示・再配信には別途Data Display and
  Licensing Agreementが必要。TipRanksデータセットへのアクセス、商用価格、更新遅延、
  銘柄カバレッジ、保存・表示・出典条件の確認を要する。契約・無料試用・キー発行は未実施。
- 許諾後は既存の非公開レビューキューへ「アナリスト評価変更」種別として接続し、証券会社、
  旧／新評価、旧／新目標価格と通貨、公表時刻、初回検知時刻、出典リンクを並べる。
  企業発表とは区別し、重複排除と人手レビューを通す。会員配信・自動公開は未接続。
- 次は既存のニュースAPI見積もり確認にFMP/TipRanks条件を加え、同一基準で比較する。

## 2026-09-24 11:10 UTC 既存候補の目標株価対応

- Stock News APIの公式例に、目標価格付きの最新格上げ／格下げを返すratings経路がある。
  既存見積もりのプランに含まれるか、全銘柄・更新遅延・顧客向け表示権を要確認。
- Benzingaは旧／新レーティングと目標価格、アナリスト・証券会社・時刻を含むAnalyst Ratings APIを
  独立したAPIとして公開。既存見積もりで同じ契約範囲か、追加料金かは未確認。
- Intrinioは価格目標データをEnterpriseの推定値製品として掲載し、公開説明は主にコンセンサス値。
  NewsEdgeのニュース記事契約とは別データ費用になる可能性を見積もりで確認する。
- finlightとAlpacaはニュース記事取得仕様を確認。目標価格アクションの構造化経路は公開仕様では
  確認できず、記事として含まれる頻度・遅延も未確定。
- 結論: ニュース提供会社を別に増やす必要があるとは限らないが、各社で専用エンドポイントや
  データ追加契約になる可能性がある。既存見積もりに含めた一括価格と表示ライセンスを確認する。

## 完了・確認済み

- 公式補完経路について、最初の障害から回復までの時間と成功を含む総試行回数を
  SQLiteへ永続保存し、運営画面へ24時間の匿名集計だけを表示する。既存履歴は
  推測で補完せず、URL・経路名・HTTPコード・例外文は公開しない。

- 開発用の常駐監視サーバー、保存領域、認証キーを既存環境から分離。
- 22銘柄の公式取得先を登録・巡回。一部エラーは残るため網羅済みとはしない。
- 補完監視17経路を常駐環境で確認済み。追加5経路はBroadcomのみ常駐成功を確認済み。
  Armは公式RSSへ切替済みで常駐再確認待ち。TSMC/Sandisk/Oracleは常駐成功未確認。
  設定上の専用補完元は18/22銘柄。MRVLは常駐環境で403のため稼働確認に含めない。
- 9/24 15:35 JSTの常駐再点検は22経路中17成功、5失敗（MRVL/TSMC 403、Arm/Oracleの応答不正、
  Sandisk timeout）。Broadcom 10件、Arm新RSSは未取得。Micronの本文取得待ちは409件。
- NBIS Platinum評価・スポット価格記事は自動取得と管理画面表示を確認。
  初回の過去記事取り込みであり、発表直後の検出実績ではない。
- 新着の取得再試行を過去記事より優先し、過去記事の処理枠も確保。
- サーバー再起動後のデータ保持を確認。CoreWeave初回取り込み待ちは解消。
- 今回Bloom EnergyとGoogle Cloudの公式RSSを追加。ローカルで10件・20件を取得確認。
  Googleは短い抜粋であり、全文取得と同一視しない。

## 次にやること（優先順）

1. Arm・TSMC・Sandisk・Oracleの4経路を常駐環境で再確認。403・timeout・不正応答を成功扱いしない。
2. MUサイトマップの初回取り込みと新着検出を実測。MRVLは常駐環境の403の正式な対応方法を確認。
3. Anthropic長文上限の修正後、次回の自動再試行で成功するか確認。
4. 残るASML・CRDO・GEV・PLTRの補完元を整備。共通元での本文言及を銘柄網羅と数えない。
5. 公表時刻が確かな新着で、公表→初回検出→画面反映を実測。
6. 関連銘柄の誤判定と重複を点検し、要約レビューへ接続。

## 現在の制限

- 05:36 UTC時点: MUの常駐本文取得成功、MRVLは403、Anthropic個別長文は修正後の再試行待ち。
- 公式IRの403・タイムアウトなども未解消。取得失敗をニュースなしと扱わない。
- 補完元は主に60〜120秒巡回。数秒の到達は未保証。
- 有料AI、自動公開、会員への外部通知はこの環境では無効。
- Railwayは無料トライアル。有料契約は行っていない。無料枠内での長期常時稼働は未確認。

## ニュースAPIの商談

| 会社 | この会話で確認できた状態 | 次の対応 |
|---|---|---|
| Stock News API | Premium商用利用・All Tickers可、通常1〜5分遅延、RESTのみ。試用拡張の申し出あり | 比較候補として保留。接続準備ができてから試用開始 |
| Benzinga | 9/22に資料案内と追って連絡する旨の返信。見積もり未着 | 元のスレッドへ価格と回答予定日の確認 |
| Intrinio / NewsEdge | 見積もり依頼文作成済み。ユーザー申告では回答待ち | 送信済みなら同じスレッドへ確認。送信状態を推測しない |
| finlight | 見積もり依頼文作成済み。ユーザー申告では回答待ち | 送信済みなら同じスレッドへ確認。送信状態を推測しない |
| Alpaca | $99個人プランは第三者向け表示不可との回答。商用条件の照会済み | 商用見積もり待ち |

9/22送信なら米国の営業日ベースでまだ約2日。提案: 9/25日本時間の夜に同一スレッドで一度催促。
内容は22銘柄と全米株の概算、最低契約期間、商用表示・AI要約の可否、回答予定日。
さらに3営業日返答がなければ公式営業窓口で担当者への取次ぎを依頼する。
これは対応案であり、メール送信・リマインダー設定を実行した記録ではない。

## ユーザーの操作

今の開発継続に必要な操作はなし。ニュースAPIの有料契約、サーバーの有料化、
試用開始を行う場合は、料金・条件を具体的に揃えて別途判断する。

## 今回続けて進めた作業（9/24）

- MU公式robots.txtが案内するsitemap.xmlから、著者・分類ページを除いた445記事を発見。
  手元と常駐環境で最初の3記事の本文取得に成功。300秒間隔で過去分を少量ずつ取り込み、
  初回取得後に現れたURLを優先。サイトマップ側の更新速度は未測定。
- MRVLの専用本文・見出し抽出を実装し、手元で3記事取得成功。常駐環境では403。
  許可されていない経路・IP切替による制限回避はせず、未解決として残す。
- 管理画面に失敗記事URL、原因、最終確認、次回再試行を表示。編集者認証が必要。
- Anthropicの失敗記事は threat-intelligence-report-september-2026。
  公開本文が16万文字の抽出上限に達することが原因。上限を維持して切り詰め表示に変更。
  末尾の未取得部分の関連銘柄・変更を検出できるとは扱わない。
- Vertivラック技術ブログRSSは本文付き9件を取得確認。冷却・ラック領域の補完であり全社網羅ではない。
- 公式投資家サイトのBroadcomニュースRSS、TSMC Press Center、Sandisk・Oracle投資家ニュース一覧、
  Arm公式ブログ一覧を追加設定。ローカルでは22経路・専用補完元18/22銘柄。
  常駐環境への反映前のため、接続・本文抽出・巡回成功は未確認。

### 2026-09-24 06:28 UTC RSS切替・ローカル確認

- Armの一覧HTMLから公式CompanyトピックRSSへ変更。作業環境でRSS本文73,860 bytes、6記事を解析。
  先頭2記事の本文も5,427文字・3,878文字抽出できた。常駐環境は再確認待ち。
- Oracleの投資家向けサイトが案内するRSSへ変更。ただし作業環境からはHTTP 200でも
  195-byteの「Site Unavailable」HTMLが返り、RSSとして使えない。常駐再試行後も同様なら
  一覧HTMLへ戻すか、Oracle側へ公式取得手段を問い合わせる。
- Broadcom RSSは作業環境で10記事を解析。常駐環境でも前回10件取得を確認済み。
- 公式URLの方式を2経路切り替え。Python 209件・JavaScript 94件成功、JSON検証・`git diff --check`成功。
- TSMCは常駐環境403、Sandisk IR一覧は常駐環境timeoutのまま。アクセス制限を回避せず、
  別の公式提供方法が見つかるまで失敗として扱う。

### 2026-09-24 06:35 UTC 常駐デプロイ再点検

- `cbb687d` を新しいRailway Previewサービスへ自動デプロイし、Onlineを確認。
- Preview管理画面で22経路中17成功。Broadcomは10件、Arm RSS切替後も`invalid-source-response`。
  TSMC/MRVLは403、Sandiskはtimeout、Oracle RSSは`invalid-source-response`。
- Micron sitemapは3件ずつ本文を処理し、残り409件。キュー総数383件は新着件数ではない。
- 管理画面が応答の理由を広いエラー分類へまとめているため、固定済みの詳細コードを出す
  診断改善を追加。次回巡回でArm/Oracleの失敗原因をさらに分類する。
- 全体テスト: Python 210件、JavaScript 94件成功。

### 2026-09-24 18:28 UTC ホスト遮断の期限後プローブ

- 2026-09-25 03:27:28 JST、期限を迎えた遮断中ホストへ本文URLを1件だけ自動再確認した。
- 再度アクセス制限となり、同じホストの残りURLへ進まず即時再遮断した。24時間集計は
  回復0・再遮断1・一時障害0。次回ホスト確認は2026-09-25 15:27:28 JST。
- 本文キューは再遮断直後に729件を同一ホスト遮断中として保留し、直近バッチは1件・
  エラー1件。SEC本文エラーはアクセス制限197件で、レート制限・タイムアウト・公式側5xx・
  添付根拠なし・その他はいずれも0件だった。
- アクセス制限は回避せず、次の期限まで追加プローブを行わない。運営画面にはURL、ホスト名、
  HTTPコード、アクセッション番号、例外文を公開しない設計を維持する。


## 2026-09-25 X監視のコード準備（未接続）

- `codex/research-preview` にX APIの読み取りアダプターを追加準備した。対象はTipRanks（`@TipRanks`）とThe Fly（`@theflynews`）。
- 22銘柄のcashtagに絞ったRecent Searchを各ソース最大10件・標準120秒間隔で取得する設計。銘柄名を含まない投稿や、対象外アカウントの投稿はレビュー候補に残さない。
- `X_API_ENABLED` と `X_BEARER_TOKEN` の両方が監視サービスに設定されない限り、ソースを巡回キューへ入れずAPIを呼ばない。既定は無効。通常の監視・ニュース配信・自動投稿には接続しない。
- X APIへの実リクエスト、クレジット購入、TokenのVercelからRailwayへの移動は未実施。API利用費も発生していない。
- X公式価格ページではPost読み取りは1件$0.005、同じPostは原則UTC日単位で重複課金を避けると説明されている。Developer Consoleのspending limitを先に設定する。現時点で予算上限は未設定。
- X Developer Policyは、利用目的を申告し、用途の実質的な変更前に承認を得ることを求めている。また、有料会員サービスでX投稿を再配信・派生要約する条件は公開情報だけで許可済みと判断できない。よってこの接続はまず非公開の編集レビュー候補まで。公開・会員配信は契約条件の確認後に判断する。
- 対象ソース: https://x.com/TipRanks 、https://x.com/theflynews
- 参照: https://docs.x.com/x-api/getting-started/pricing 、https://docs.x.com/developer-terms/policy 、https://docs.x.com/developer-terms/agreement

### 2026-09-25 MU決算の検出時刻比較

- 現在のRailway `research-staging` はXの1日2300リクエスト上限を維持。通常120秒間隔の3社では2160回/24時間、終日60秒では4320回となり途中で上限に達する。
- MU決算に合わせ、10月1日04:30〜05:50 JSTのみ3社を60秒間隔にし、前後は120秒に戻す設定をプレビュー側に追加。境界分を含む上限見積りは2286回/24時間で、2300回の設定を変更しない。料金は取得されたポスト数に依存し、API上限は金額上限ではない。
- Micron公式による9月30日14:30 MDT（日本時間10月1日05:30）の予定は決算説明会の時刻。決算資料の実際の公開時刻には使わない。公開後に公式リリースの時刻を確認し、X投稿日と本サービスの初検出時刻を別々に比較する。
- 3社へのニュースAPIの問い合わせ・追送はユーザーが実施済み。返信を待ってニュース配信の契約条件を判断する。

### 2026-09-25 目標株価の秒数比較・X限定の対象追加

- 3社のX監視は目標株価関連と決算関連の投稿だけを保存。企業の一般ニュースは別の公式経路の対象で、Xから一般ニュース全体を取得していない。
- Xだけの試行対象にLITE・COHR・VSTを追加し、X監視は25銘柄とした。通常の企業調査・公式ソース監視の22銘柄は変更しない。既存の3検索・120秒間隔・MU発表時限定の60秒間隔・1日2300回の上限はそのまま。各検索文字列は512文字以内。
- Xからの取得料金はリクエスト回数ではなく返された投稿数によるため、対象拡大で料金が増える可能性がある。各検索は最新10件までであり、投稿集中時は取りこぼしを独立の比較元で確認する必要がある。追加3銘柄の結果を見てから次の拡大を判断する。
- 目標株価の比較レポートに投稿URL・X投稿時刻・初検出時刻・投稿から検出までの秒数を個別掲載。証券会社が顧客に配信した正確な時刻はX投稿からは分からず、元資料が時刻付きで確認できる場合だけ別途照合する。

### 2026-09-25 X限定の監視対象を33銘柄へ拡大

- ユーザー候補のIREN・ALAB・APH・VST・INTC・ARM・AMAT・SIMO・AAOI・METAのうち、ARMとVSTは既存の25銘柄に含まれるため、追加は8銘柄、合計33銘柄。
- TipRanks・The Fly・Wall St Engineの3検索に追加した。目標株価と決算に関係する投稿だけを非公開の確認用に保存し、公式ソースで追う22銘柄や一般ニュース配信には追加しない。
- 検索文の長さは498・500・502文字で、Xの上限512文字以内。3検索・120秒間隔・最大10件・MU決算時間帯のみ60秒・1日2300回のリクエスト上限は変更しない。
- リクエスト回数を増やさなくても、検索で返るポストが増えれば読取費用は増える。ポストはローカルで絞り込む前にXから返され、最大10件に達した巡回の見逃しは別途点検する。検索文の残りは最長のWall St Engineで10文字なので、さらに広げる場合は構成を再検討する。
- VRT（Vertiv）とVST（Vistra）は別の銘柄として3発信元すべてのX検索に登録済み。非公開の取得試験は、本番サイトで対象銘柄の目標株価変更を通知するための精度・速度・費用の検証として続ける。試験データをそのまま会員へ自動配信することはせず、発信元・証券会社名・新旧目標株価・格付け・日付の確認と配信条件を詰める。
- Benzingaの2026-09-25の返信で、Premium U.S. Stocks News APIは月$5,750、Analyst Ratings APIは月$3,700と提示された。AI利用料は各API商品へ別途適用される（前便の基本方針は30%）。ユーザーはこの価格では契約しない判断。2週間までの試用キーも提示されたが、料金の前提が合わないため依頼しない。他社の回答とX試験を継続する。

### 2026-09-25 20:02 UTC 新着本文待機時間とタイムアウト再試行

- 本文未取得730件を、新着検知39件と履歴基準691件に分離。新着判定は永続化済みの
  `release_events` に限定し、過去取り込みを推測で新着扱いしない。
- 新着検知39件はすべて検知時刻を検証できた。最古は2026-09-24 06:07:26 UTC、
  2026-09-25 20:01:40 UTC時点の最大待機は136,454,158ms。URL、本文、ホスト名は公開しない。
- 未来、タイムゾーンなし、不正、31日超の検知時刻は待機時間から除外し、件数だけを
  「時刻検証不可」として表示する。待機時間は内部キューの観測値で、取得・配信の保証ではない。
- 2026-09-26 05:01 JST、期限を迎えた公式補完のタイムアウト経路が自動で22回目を試行したが
  未回復。次回枠は2026-09-26 09:16:31 JSTへ更新された。早期通信やアクセス制限の回避は行わない。
- 公式補完は19/22正常。残りはアクセス制限2件とタイムアウト1件。本文ホストの単一復旧
  プローブは2026-09-26 15:27:28 JST以降で、SEC資料198件はアクセス制限中。
- 次は09:16 JST以降のタイムアウト回復実測と、15:27 JST以降の本文ホスト単一プローブを
  それぞれ期限前に通信せず確認する。

### 2026-09-25 20:54 UTC 新着本文キューの公平性

- 本文バッチの先頭は従来どおり最新の未取得新着を優先し、2件以上の枠がある場合は
  空き1枠を検証可能な検知時刻を持つ最古の未取得新着へ予約する。新着が継続しても
  古い新着が無期限に後回しにならない。
- 期限到来済みのホスト復旧プローブは公平性枠より優先し、同一ホストは1バッチ1件の制限を維持。
  不正、未来、タイムゾーンなし、31日超の検知時刻は公平性枠の選択根拠にしない。
- デプロイ後の実環境はready、優先5銘柄5/5正常、公式補完19/22正常。本文キュー926件は
  未取得731件（新着検知40、履歴基準691）、抽出不足0件、抽出済み195件。新着40件は
  すべて時刻検証可能だった。
- 現在は同一ホスト遮断により730件が保留、取得可能0件のため、公平性枠による実取得は
  次の合法的なホスト回復後に観測する。アクセス制限の回避や期限前の通信は行わない。
- SEC資料は199件中、アクセス制限198件、未処理1件。本文ホストの単一復旧プローブは
  2026-09-26 15:27:28 JST以降。公式補完タイムアウト経路の次回枠は09:16:31 JST。
- 検証: Python 269件、画面・データ95件、lint、TypeScript、本番ビルド35ページ、
  `git diff --check` 成功。Vercelと隔離Railwayも最終コミットまで成功。

### 2026-09-25 04:43 UTC X API日次上限の公平なペーシング

- 先行追加されたMU決算時間帯の3発信元60秒監視を保持。日次上限2,300回では
  保守的な設定上限2,286回/24時間に収まるため、ペーシングを追加せず高速時間帯を維持する。
- 設定需要が日次上限を超える場合だけ送信を均等化。既定100回/24時間では
  API全体を864秒以上、3発信元を公平に回す場合は各発信元を概ね2,592秒以上空ける。
- SQLiteの排他トランザクション内で上限確認・公平な発信元選択・予約を一体化し、
  複数ワーカーでも同時超過や特定発信元への偏りを避ける。24時間ちょうど前の試行は枠から除外する。
- 運営画面には集約済みの間隔・次回送信枠だけを表示し、検索文、URL、Token、応答本文は公開しない。
  X APIは既定OFF、読取専用、非公開、人間レビュー必須、自動公開なしのまま。
- KLAC、LITE、MRVL、NBIS、NOW、NVDA、ORCL、PANW、PLTR、SKHYとBLS・FOMCを公式情報で再確認。
  将来の確定決算日は裏づけられず、推定日や `lastCheckedOn` は追加していない。
- 検証: Python 236件、画面・データ94件、lint、TypeScript、本番ビルド35ページ、`git diff --check` 成功。

### 2026-09-25 05:53 UTC Palantir株主書簡の公式補完

- 既存のPalantir公式プレスリリース・サイトマップ監視とは別に、公式サイトマップから
  `/qN-YYYY-letter/en/` に完全一致する英語版株主書簡だけを抽出する補完経路を追加した。
- 公開HTML内の `__NEXT_DATA__` からContentfulのリッチテキスト葉だけを読み取り、JavaScriptは実行しない。
  他言語、ランディングページ、メディア記事、外部ドメイン、Contentfulメタデータは証拠から除外する。
- 公式サイトへの実取得でQ2 2026、Q1 2026、Q4 2025の3書簡を取得し、本文11,611〜19,457文字、
  PLTR固定関連付け、本文取得エラー0件を確認した。初回は過去資料として扱い、新着とは表示しない。
- SNOW、VRT、AAPL、AMZN、ANET、ARM、AVGO、BE、COHR、CRDOとBLS・FOMCを公式情報で再確認。
  将来の確定決算日は裏づけられず、推定日や `lastCheckedOn` は追加していない。

### 2026-09-25 13:55 UTC 子記事回復時間の永続実測

- 公式HTML一覧・サイトマップ配下の子記事について、最初の失敗時刻と連続試行数を
  非公開状態へ保存し、回復時に成功した要求を含む総試行回数と回復所要時間を確定する。
- 回復標本はURL、HTTPコード、応答本文、例外文を含めず100件に制限。運営画面には
  24時間の件数、平均・最大所要時間、平均・最大試行数、最終回復時刻だけを表示する。
- タイムゾーンなし、不正時刻、未来時刻、7日超の測定は公開集計から除外する。
  表示値は実測であり、再試行時刻や購読者への配信時間を保証しない。
- PLTR、SKHY、SNOW、VRT、AAPL、AMZN、ANET、ARM、AVGO、BEとBLS・FOMCを
  公式情報で再確認。将来の確定決算日は裏づけられず、推定日や `lastCheckedOn` は追加していない。

### 2026-09-25 21:56 UTC 同一ホスト内の新着本文公平性

- 前回の最古新着予約は、最新記事と最古記事が同じホストの場合、1バッチ1ホスト1件の
  制限により最古記事が選ばれない余地があった。同一ホスト競合時は最古の有効新着へ
  そのホストの枠を切り替え、最新記事は次回の取得候補として残すよう修正した。
- 期限到来したホスト復旧プローブを優先し、1バッチ1ホスト1件の制限は維持する。
  運営画面にはURLやホスト名を出さず、最古枠の予約有無、検証済み待機時間、
  同一ホスト内切替の有無だけを表示する。
- デプロイ後はready、優先5銘柄5/5正常、初回確認8.506秒、公式補完19/22正常。
  残りはアクセス制限2件・タイムアウト1件で、タイムアウトの次回枠は
  2026-09-26 09:16:31 JST。約10時間28分・22試行の実測中で、回復はまだ確認できない。
- 本文キュー926件は未取得731件（新着40、履歴691）、抽出不足0件、抽出済み195件。
  同一ホスト遮断730件・取得可能0件のため、新しい同一ホスト公平処理の実動作は
  2026-09-26 15:27:28 JST以降の合法的な単一復旧プローブ後に確認する。
- SEC資料199件はアクセス制限198件・未処理1件。アクセス制限の回避や期限前通信は行わない。
- CRDO、CRM、CRWV、GOOGL、KLAC、LITE、MRVL、NBIS、NOW、NVDAとBLS・FOMCを
  公式情報で再確認。確定決算日の追加根拠はなく、取得不能・空欄は保留した。
- 検証: Python 270件、画面・データ95件、lint、TypeScript、本番ビルド35ページ、
  `git diff --check` 成功。Vercelと隔離Railwayも最終コード・カレンダーコミットまで成功。
